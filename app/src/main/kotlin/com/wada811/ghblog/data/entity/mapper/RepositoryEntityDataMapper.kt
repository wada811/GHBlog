package com.wada811.ghblog.data.entity

import com.wada811.ghblog.model.domain.Repository

object RepositoryEntityDataMapper {
    fun transform(entity: RepositoryEntity): Repository = Repository(
            entity.id,
            entity.name,
            entity.full_name,
            OwnerEntityDataMapper.transform(entity.owner),
            entity.private,
            entity.html_url,
            entity.description,
            entity.fork,
            entity.url,
            entity.forks_url,
            entity.keys_url,
            entity.collaborators_url,
            entity.teams_url,
            entity.hooks_url,
            entity.issue_events_url,
            entity.events_url,
            entity.assignees_url,
            entity.branches_url,
            entity.tags_url,
            entity.blobs_url,
            entity.git_tags_url,
            entity.git_refs_url,
            entity.trees_url,
            entity.statuses_url,
            entity.languages_url,
            entity.stargazers_url,
            entity.contributors_url,
            entity.subscribers_url,
            entity.subscription_url,
            entity.commits_url,
            entity.git_commits_url,
            entity.comments_url,
            entity.issue_comment_url,
            entity.contents_url,
            entity.compare_url,
            entity.merges_url,
            entity.archive_url,
            entity.downloads_url,
            entity.issues_url,
            entity.pulls_url,
            entity.milestones_url,
            entity.notifications_url,
            entity.labels_url,
            entity.releases_url,
            entity.deployments_url,
            entity.created_at,
            entity.updated_at,
            entity.pushed_at,
            entity.git_url,
            entity.ssh_url,
            entity.clone_url,
            entity.svn_url,
            entity.homepage,
            entity.size,
            entity.stargazers_count,
            entity.watchers_count,
            entity.language,
            entity.has_issues,
            entity.has_downloads,
            entity.has_wiki,
            entity.has_pages,
            entity.forks_count,
            entity.mirror_url,
            entity.open_issues_count,
            entity.forks,
            entity.open_issues,
            entity.watchers,
            entity.default_branch,
            PermissionEntityDataMapper.transform(entity.permissions)
    )
}
